import TimeDisplay from "app/core/components/TimeDisplay"
import React from "react"

interface Props {}

const Counter = (props: Props) => {
  return <TimeDisplay>0'00</TimeDisplay>
}

export default Counter
